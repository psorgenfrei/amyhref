- @auth.each do |key, value|
  h1= key 
  p= value

hr

= @user.inspect
br
= @token.inspect

hr

= "Seeing #{@inbox_messages_count} messages in INBOX"
br
= "Seeing #{@all_messages_count} messagas in TOTAL"
